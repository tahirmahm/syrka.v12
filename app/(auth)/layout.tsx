/**
 * Scaffolding for the Phase 2 authentication placeholder (mock sign-in).
 * No page.tsx yet — built when the auth screens are designed in Phase 2.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
