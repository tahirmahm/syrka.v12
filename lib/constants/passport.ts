/**
 * Presentation-layer label for the Campus student-facing Passport surface.
 * The underlying domain concept stays the generic Passport type
 * (lib/types/passport.ts) per ADR-002/ONT-001; only the UI label differs
 * by surface. Do not rename the type — change this constant instead.
 */
export const PASSPORT_DISPLAY_NAME = 'Academic Passport'
